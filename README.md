# Number of Digits Counter C

This C program asks the user to enter an integer and then calculates how many digits the number contains.
The digit count is performed by repeatedly dividing the number by 10 until it becomes 0.
